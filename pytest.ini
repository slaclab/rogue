[pytest]
markers =
    integration: fixed-port or network-heavy integration tests
    epics: EPICS/PVA dependent tests
    perf: soak, throughput, or benchmark-style tests
    rocev2: hardware-gated tests requiring an ibverbs device (real NIC or rdma_rxe)
